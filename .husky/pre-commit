#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run schema validation tests before allowing commit
echo "🔍 Running schema validation tests..."

npm run test:schema
SCHEMA_EXIT=$?

npm run test:cli
CLI_EXIT=$?

if [ $SCHEMA_EXIT -ne 0 ] || [ $CLI_EXIT -ne 0 ]; then
  echo ""
  echo "❌ Schema validation tests failed!"
  echo "Please fix the errors before committing."
  echo ""
  exit 1
fi

echo "✅ All schema validation tests passed!"
echo ""
